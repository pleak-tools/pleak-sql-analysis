"input.db";
velocity latitude longitude;
x4 = exp -0.5 velocity;
x5 = lp 2.0 latitude longitude;
x6 = prod x4 x5;
return selectMax x6;

//SELECT MAX e^{-0.5*velocity} * sqrt(latitude^2 + longitude^2) FROM input.db
