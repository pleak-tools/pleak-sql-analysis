* preprocess.py takes the tables produced by 'dbgen' application of TCP-H dataset and formats them appropriately. it can be applied to tables with larger scale factors.
* To run larger instances of TCP_H dataset with larger scale factors, it is recommended to write data directly into the database instead of using --db-create-tables.
